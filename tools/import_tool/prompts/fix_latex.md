В тексте ниже найди математические выражения и формулы, которые написаны без знаков $ (или написаны некорректно), и расставь их:
- Для inline-формул (внутри предложения) используй одиночные $...$
- Для display-формул (отдельная строка или крупное выражение) используй $$...$$

Не трогай:
- Текст, где $ уже расставлены корректно
- Обычный текст без математики
- Блоки кода (```)
- YAML frontmatter (между --- в начале файла)

Верни ТОЛЬКО исправленный текст без объяснений и без форматирования markdown.
